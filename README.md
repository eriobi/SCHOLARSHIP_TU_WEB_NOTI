วิธีใช้งาน
1. ติดตั้ง docker
2. git clone code
3. ไปที่โฟลเดอร์ที่เก็บ code แล้ว docker compose up --build
กรณีที่ขึ้น /app/node_modules/.bin/nodemon: 1: ../nodemon/bin/nodemon.js: not found ให้ทำการลบ node_modules และใช้คำสั่ง npm i อีกครั้งจพสามารถทำงานได้
